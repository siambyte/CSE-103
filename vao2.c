#include "stdio.h"
void main()
{
    int length=20,width=10,perimeter,area;
    perimeter=2*(length+width);
    area=(length*width);
    printf("area is=%d\n",area);
    printf("perimeter is=%d",perimeter);
}
